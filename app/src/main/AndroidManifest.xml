<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.beta.cuckoo">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- Internet Permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/cuckoologo1"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/cuckoologo1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.beta.cuckoo.View.Splash.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/cuckoologo1" />
        <!--
 Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <service
            android:name="com.beta.cuckoo.FirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name="com.beta.cuckoo.View.LabelImage" />
        <activity android:name="com.beta.cuckoo.View.UserStats.UserStatsDetail" />
        <activity android:name="com.beta.cuckoo.View.UserInfoView.UserShow" />
        <activity android:name="com.beta.cuckoo.View.PostDetail.PostDetailCommentSendImage" />
        <activity android:name="com.beta.cuckoo.View.Locations.SeeUsersLocation" />
        <activity android:name="com.beta.cuckoo.View.Locations.UpdateLocation" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name="com.beta.cuckoo.View.ExploreLocation" />
        <activity android:name="com.beta.cuckoo.View.UserInfoView.ProfileDetail" />
        <activity android:name="com.beta.cuckoo.View.Chat.SearchUserToChatWith" />
        <activity android:name="com.beta.cuckoo.View.Chat.ChatSendImage" />
        <activity
            android:name="com.beta.cuckoo.View.Chat.Chat"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name="com.beta.cuckoo.View.UpdateUserInfo.UpdateCoverPhoto" />
        <activity android:name="com.beta.cuckoo.View.UpdateUserInfo.UpdateAvatar" />
        <activity android:name="com.beta.cuckoo.View.WelcomeView.SignUp" />
        <activity
            android:name="com.beta.cuckoo.View.MainMenu.MainMenu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.beta.cuckoo.View.PostDetail.PostDetail" />
        <activity android:name="com.beta.cuckoo.View.WelcomeView.LoginActivity" />
        <activity android:name="com.beta.cuckoo.View.WelcomeView.MainActivity" />
    </application>

</manifest>